node_modules
dist
.DS_Store

# Astro content site — build artifacts only (source lives in content-site/)
content-site/.astro/
public/learn/
*.log
.env
.env.local
.env.*.local
.dev.vars
.wrangler
test-results
playwright-report
.worktrees/
.superpowers/
scripts/output
scripts/chart-lookbook/clients/
scripts/chart-lookbook/*-lookbook.html
scripts/chart-lookbook/*-lookbook.pdf

# LED firmware — belongs in the led/ project, not the oracle repo
LED-Programming/

# Local Claude session config — but keep committed slash commands shared
.claude/*
!.claude/commands/
!.claude/commands/**

# Local R2 backups (scripts/backup-atlas.ts) — stewards.json holds collector
# emails; these must never be committed
backups/

# Conductor: settings.toml is shared (tracked); settings.local.toml is each
# teammate's machine-specific override and wins over the shared file.
.conductor/settings.local.toml
scripts/.refill-tmp/
