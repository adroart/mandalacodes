node_modules
dist
.DS_Store
*.log
.env
.env.local
.env.*.local
.dev.vars
.wrangler
test-results
playwright-report
scripts/output

# LED firmware — belongs in the led/ project, not the oracle repo
LED-Programming/

# Local Claude session config
.claude/
