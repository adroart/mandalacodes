# 22 September reliability repairs

Scope: confirmed defects from the cross-site audit, verified against freshly fetched
`origin/main` (`b9da484cc5b973de2a82bd11ef01ada35dce8fde`).
Branch: `codex/mandala-audit-repairs`. This implements existing honest-boundary
and offline-reader plans. It does not reopen collector launch, manuscript writing,
or bodygraph scope.

## Finding disposition

| Finding | Implemented behavior | Evidence / remaining boundary |
| --- | --- | --- |
| MC-01 guest loss | Preserve guest chart across signed-out reload; account-specific slots hide another owner's chart synchronously. | `profileContinuity.lifecycle`, `audit-reader-boundaries`, channel/relations browser cases. |
| MC-02 first sync | GET 204 means empty; claim guest and PUT; retry action after failure. | Lifecycle unit tests and browser rejected first-upload case. |
| MC-03 share codec | v2 three-hex-character slots cover every gate/line; valid v1 remains readable; malformed input rejects. | `profileShareCodec`: all 11 positions × 64 gates × 6 lines. |
| MC-04 hidden recorder/Safari | Current visible deck link accepts existing administrator hold; visible Hold to record hint; Space records, Enter/tap opens deck; opaque recorder replaces hidden ordinary footer. Safari QR handoff lives outside hidden nav. | 52 recorder cases across desktop/mobile; screenshots visually inspected. Synthetic microphone/API only. |
| MC-05 unverified administrator | Verified email AND allowlist required. | `adminAuthContract`, reflection/invocation API tests. |
| MC-06 false profile success | Check PUT/DELETE status, retain local data after rejection, show sync error/retry, discard stale account completions. | Lifecycle and browser cases. No new delete UI. |
| MC-07 false collections success | Check every mutation response; preserve existing state on failure; show error; isolate owner switches. | Collections browser and owner-isolation unit cases. |
| MC-08 retired dependencies | Immediate no-input claim/edit/homecoming/make boundary. Active Atlas/piece readers stop requesting retired catalogue/private certificate. Designs and physical instances have distinct routes/wording. | Boundary/browser cases, canonical adapter tests, art-link contract tests. Art registration remains closed. |
| MC-09 false empty pieces | Missing DB/tables return 503; other read failures return 500; only a real empty result returns empty success. | `accountPieces`, existing browser empty-state test. |
| MC-10 nonblocking mobile CI | Repaired locally: current-frame accessibility and assertions, production-worker lane, failing browser job now fails workflow. | Final counts below. GitHub execution and required-check configuration remain publication follow-ups. |
| MC-11 authoring notes | Remove only three bracketed expansion instructions from public Systems page; retain draft disclaimer and authored prose. | Diff review. Editorial expansion stays with Adrian. |
| MC-12 Learn service worker | Exclude separate Learn/designer documents from Oracle SPA navigation fallback; production-preview directory parity. | Installed, controlling-worker Learn index/article/reload/return test. |
| Additional bounded gaps | Retry city-index outages truthfully; partial offline warming retries failed prose and runs per document; dialog focus/escape/return behavior. | City browser/unit and offline recovery tests; focus hook integrated in existing overlays. |

## Verification recorded before the first repair commit

- Complete unit suite: **78 files, 828 tests passed**.
- `npm run typecheck`: app and media worker passed; worker uses its own Cloudflare
  types instead of being silently omitted from verification.
- `npm run verify:profile`: all 11 positions match the existing reference fixture.
  This is one fixture, not a scientific/timezone breadth claim.
- Complete `npm run build`: Oracle corpus, search, prose check, Astro Learn,
  sitemap, Vite/PWA, 64 card prerenders and CSP succeeded. After the final recorder
  visual fix, Vite + prerender + CSP were rebuilt, and units/typecheck repeated.
- Production PWA Learn: **1 passed**, with a controlling service worker checked
  before Learn navigation and after entry. API/media are synthetic.
- Repair-focused mobile run across 11 files: **86 passed, 1 desktop-only skipped**.
- Final recorder suite after screenshot correction: **52 passed**, Desktop Chrome
  and Mobile Chrome. Keyboard test captures hint and active recorder screenshots.
- Earlier complete Mobile Chrome run: **102 passed, 31 failed, 11 skipped**.
  This preceded test-contract alignment. Do not present focused passing counts
  as a green complete lane. The frozen first-commit rerun at `ac1d628` recorded
  **126 passed, 8 failed, 11 skipped**, narrowing failures to the cases below.
- Actual Ego browser inspected local design/boundary/card links and visible admin
  hint/keyboard affordance at mobile width. Synthetic recording error controls were
  reachable; successful recording persistence is proved by Playwright fixtures.
  Ego screenshot API timed out, so Playwright screenshots supplied pixel review.

## Mobile failure disposition

- **Baseline stale expectations, source evidence at unchanged b9da484:** entry
  invitation was already Enter your birth time / Your placement is illuminated
  and navigated to profile; tests expected old Your codes copy and Atlas.
  Coin result already said Hexagram 22 · Grace / Turning into / Open that card.
  Both files reproduced failures on an isolated server; exact unchanged HEAD host
  source confirmed labels before alignment. Seven aligned cases pass. No clean-base
  browser execution is claimed.
- **Baseline fixture mismatch:** owned-pieces acquisition already used signed-in
  handoff. Atlas theme cases supplied no valid canonical response even though the
  reader rejects malformed responses. Tests now provide synthetic canonical empty
  state and preserve visible stage, paper, and ink assertions.
- **Repair-driven contract changes:** retired forms now disclose boundary before
  input and make zero retired requests; tests retain destination and page-health
  checks. Channel/relations fixtures now use the actual profile GET 204 contract,
  previously incorrectly 404. Safari asserts the visible current frame and external
  handoff. Deck → artwork → Back uses a synthetic destination document with a visible
  heading; it does not contact production.
- **Integration regression repaired:** screenshot review found transparent recorder
  overlap; applied current reader theme to recorder and hide underlying footer while
  recording. Visibility, nontransparent background, exit/focus and screenshots checked.
- **Production-worker cases:** offline QR and Learn now execute in the explicit
  built-worker lane, with all three cases exercised rather than skipped in dev.
- **Current-frame contracts:** visual/typography cases now inspect the visible
  frame, retain tap targets, destinations, non-overlap, prose measure and typography,
  and assert accessible system navigation and live scroll progress. Generated and
  authoritative design source both expose those semantics.
- **Acceptance delta:** Pixel 5 recorder state clipping is corrected with explicit
  typography precedence and more center-cell space; 44px controls remain intact.
  Recording, saved, transcription retry and microphone-pending states are checked
  on mobile/desktop, with recording also at 320px. Exact final evidence follows.
- **Scroll-timing fixture repair:** two intervening full runs each recorded
  131 passed / 1 failed / 10 skipped, in different channel cases. The helper
  scrolled before the current frame's 500ms chapter animation finished. It now
  waits for the chapter at the rail, uses the actual element scroll operation,
  retains viewport/opacity assertions, and checks the return URL after Back.
  All seven channel cases passed twice together (14/14) before the final lane.
- **Discarded interrupted evidence:** two intermediate browser runs were disturbed
  by Vite full reloads while source/build files changed. Frozen focused checks above
  supersede those interrupted results; isolated retries alone were not called full pass.

## Atlas acceptance follow-up

Independent actual-handler acceptance proved consent publishes a city and
withdrawal removes it; no stale-city leak was observed. A founding light counts
a claimed piece and remains counted when its location is private. Atlas now
conditions the desktop map invitation on eligible piece markers under the current
filters, excluding the visitor's birth-origin marker. Browser checks preserve two
claimed lights while withdrawing both actual fallback markers and the invitation.

Registry wording uses a positive integer founding ordinal as evidence of a held
piece: public cities retain their normal status, held pieces without a public city
read “held; location private”, and missing/invalid identity stays neutral. “Waiting
for someone” requires explicit existing catalogue availability; an available
catalogue entry cannot override a canonical state record. This is optional internal
row metadata, not a public API/schema or custody change. The place column likewise
avoids calling a private or unknown location “not yet placed”. Unit cases cover
public/private/available/unknown separately; desktop/mobile browser cases replay
public → private → unknown. Claimed totals, ordinals and authored prose are unchanged.

## Final local verification and acceptance delta

- Complete unit suite: **78 files, 832 tests passed**
  (`/tmp/mandala-final-integrated-unit.log`).
- App + media-worker typechecks passed
  (`/tmp/mandala-final-integrated-typecheck.log`).
- Final source Vite/PWA build, 64 prerenders and CSP generation passed
  (`/tmp/mandala-final-integrated-build.log`). Astro/corpus/search inputs did not
  change after the complete first-tranche build and independent acceptance build.
- Complete Mobile Chrome lane: **135 passed, 10 existing conditional skips, zero failures (3.4 minutes)**
  (`/tmp/mandala-integrated-mobile.log`). The ten existing conditional skips are
  six live-production smoke cases, one real-Functions CSP case, one opt-in
  prerender case, one desktop-only profile layout and one disabled-feature branch.
  No failing case was skipped or given a longer timeout.
- Final production-worker lane: **3 passed**
  (`/tmp/mandala-integrated-pwa.log`): offline cached arrival, uncached retry, and
  controlling-worker Learn index/article/return navigation.
- Final desktop recorder state delta: **4 passed**
  (`/tmp/mandala-final-desktop-recorder.log`); all mobile recorder checks are in
  the complete lane. Saved screenshots were visually inspected.
- Atlas browser delta: **10 passed** across desktop/mobile before the final
  neutral unknown-place refinement; final desktop registry **1 passed**, and the
  final mobile case runs in the full lane. Claims remain counted after withdrawal,
  actual fallback marker circles disappear, the invitation disappears, and private
  or unknown records are not described as unclaimed.
- Local Pages Functions compilation succeeded and resolved the correct auth graph
  as detailed below. `npm run lint:tells` completed as the existing report-only gate.
- Exact tested source is the second repair commit containing this report, on top
  of `ac1d628b7cd2d87248a77503ea779c87e5a26a5c`; its full SHA is recorded in the
  manager handoff. No source edits followed the final integrated browser run.
- Temporary preview listeners from this task were stopped and the Ego taskspace
  was finished. Browser media and APIs are synthetic; actual artwork/provider
  delivery are not certified by the screenshots.

Independent acceptance on `ac1d628` passed 46 focused contracts, both actual
cross-repository handoff directions with synthetic SQLite sessions, installed-worker
Learn navigation, and desktop/mobile card → Art design → same-card journeys.
Its recorder clipping finding is addressed in this increment; provider calls remain
synthetic and production deployment is not claimed.

The auth loader finding is dispositioned without changing the working auth flow.
`node --import tsx` substitutes the TypeScript sibling for an explicit `auth.js`
import; the two existing helpers intentionally have different consumer contracts.
Wrangler **4.108.0** successfully compiled the actual Pages Functions graph. Its
esbuild metafile resolves both `api/auth/handoff.js` and
`api/auth/handoff/accept.js` to **`api/_lib/auth.js`** and includes its
`safeReturnPath` implementation. The typed admin routes separately use `auth.ts`.
Vitest and the actual deployment bundler agree, so adding duplicate exports or
rewriting auth to satisfy the alternate loader is unwarranted. Use the proven
Vitest paired-contract harness for Node acceptance. Local evidence:
`/tmp/mandala-auth-resolution.json`, `/tmp/mandala-functions-auth-metafile.json`.
Reproduce without deployment:

```sh
WRANGLER_SEND_METRICS=false npx wrangler pages functions build \
  --outdir /tmp/mandala-functions-auth-audit \
  --metafile /tmp/mandala-functions-auth-metafile.json \
  --compatibility-date 2024-09-23 --compatibility-flags nodejs_compat
```

The mobile job no longer uses `continue-on-error`; it builds once, runs the dev
mobile suite, then the production-worker suite. This makes failures fail the
workflow. It does **not** configure GitHub branch protection: the protection API
returned 403 (plan/repository visibility restriction). A required-check claim awaits
repository-owner configuration and a published GitHub run.

## Cross-site contract and limits

Design links are `/creations/<catalogue-id>?from=mandalacodes&card=N`, using explicit
cardNumber metadata (card 1 is UL-122). Art validates the pair and owns the fixed
return to `https://mandalacodes.com/universal-language/N`, including slug redirect.
Physical links use `/works/<artworkId>?instance=<AR-code>` only when canonical public
state supplies the actual code. Never derive a physical code from a catalogue ID.

No art-repository changes, D1 migrations, launch flag changes, real sign-in emails,
payments or metered provider calls were authorized in this repair worktree.
Physical registration/recovery, authenticated deployed handoff, actual provider
success, production schema/bindings and complete restoration need separate proof.
The existing cross-site profile schema and Oracle manuscript remain unchanged.
Publication is held at the coordinator's pending export approval; no push, PR,
merge or deployment has occurred from this branch.

## Reproduce locally

```sh
npm ci
npm run test:unit
npm run typecheck
npm run verify:profile
npm run build
npm run test:pwa
CI=true PLAYWRIGHT_BASE_URL=http://127.0.0.1:2442 npm run test:mobile -- --workers=2 --output=/tmp/mandala-mobile-artifacts
```

For a safe read-only acceptance preview after build:
`AUDIT_PORT=2429 node tests/support/audit-preview.mjs`.
It binds loopback, stubs media and APIs, and never forwards provider requests.
Use `x-audit-admin: synthetic` only for the synthetic recorder capability; a real
microphone/provider is neither required nor proved by this server.

## Work ownership

Astra manager integrated/reviewed source, PWA/offline/city recovery, route/design
boundaries, canonical mapping and adapter, focus handling, browser proof and delivery.
At most two implementation workers ran concurrently:

- `profile_continuity` / `gpt-5.6-sol`: profile lifecycle/storage, recorder/Safari
  restoration, assigned account/theme/entrance browser contracts, current-frame
  accessibility and visual assertions, recorder label geometry.
- `auth_share` / `gpt-5.6-terra`: verified-admin predicate, share codec, collections
  and owned-pieces error handling, assigned boundary/channel/entry/cast tests,
  Atlas invitation/marker withdrawal and registry status helpers.

Manager review corrected the initial unverified-admin predicate, removed an
unrequested delete UI, fixed stale deletion completion and recorder visual overlap,
and retained synthetic destination health assertions.
