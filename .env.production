# Customer accounts use self-owned Better Auth (email code + password + Google),
# same-origin, NO public build-time key needed.
# Runtime secrets live in Cloudflare Pages env (BETTER_AUTH_SECRET, BETTER_AUTH_URL,
# RESEND_API_KEY, RESEND_FROM_EMAIL, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, ADMIN_EMAILS).
# (Old Clerk publishable key removed when login moved in-house.)
